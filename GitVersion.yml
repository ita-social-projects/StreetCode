mode: Mainline
branches:
  feature:
    regex: ^feature?[/-]
    source-branches: ["master", "main"]
    tag: feat-{BranchName}
  fix:
    regex: bugfix(es)?[/-]
    source-branches: ["master", "main"]
    tag: bugfix-{BranchName}
ignore:
  sha: []
merge-message-formats: {}
